interface:
  display_name: "WebMCP AI CLI"
  short_description: "Run local AI providers through one safe CLI"
  default_prompt: "Use $webmcp-ai-cli to inspect providers and run a structured AI generation safely."
